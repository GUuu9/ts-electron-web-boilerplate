## 📅 2026년 4월 15일 (수)

### 1. 프로젝트 초기화 및 환경 구축
- **실행 명령어**:
  ```bash
  npm init -y
  npm install --save-dev typescript electron electron-builder vite vitest @types/node
  npx tsc --init
  mkdir -p src/core/database src/core/di src/core/network src/core/route src/features src/shared src/renderer/public src/renderer/src
  ```

### 2. Node.js 런타임 업그레이드 및 보안 패치
- **변경 사양**: Node.js v20 -> **v22.22.2 (LTS)**

### 3. TypeScript 설정 최적화 (.json)
- **수정 파일**: `tsconfig.electron.json`
- **핵심 코드**: `"module": "NodeNext", "moduleResolution": "NodeNext"`

### 4. 의존성 주입(DI) 시스템 구현
- **핵심 코드**: 싱글톤 컨테이너 구현 및 `CalcService` 등록.

### 5. 환경 변수 및 보안 관리 구축
- **실행 명령어**: `npm install dotenv`
- **수정 파일**: `src/main.ts`, `.env`, `.gitignore`

### 6. 멀티 플랫폼 빌드 명령어 고도화
- **수정 파일**: `package.json` (`"dist": "..."`)

### 7. 네트워크 인프라(Network Infra) 구축
- **HTTP/TCP/UDP/Socket.io**: REST API, 저수준 스트림, 패킷 통신, 실시간 이벤트 클라이언트 구현 및 DI 등록 완료.

### 8. OS별 네트워크 권한 설정 (Permissions)
- **macOS Sandbox**: `entitlements.mac.plist` 설정을 통한 네트워크 권한 허용 및 가이드 작성.

### 9. 코어 기능 호환성 관리 (Web vs Desktop)
- **조건부 등록(Conditional Registration)**: `DIContainer`에서 `isElectron` 여부를 감지하여 플랫폼별 서비스 필터링.

### 10. 프로젝트 ESM 전환 (Vite 6 호환성 강화)
- **ESM 선언**: `package.json`에 `"type": "module"` 추가 및 프로젝트 전반의 경로 별칭(@) 적용 및 확장자 명시 에러 해결.

### 11. 장치 제어 인프라(Device Control) 구축
- **블루투스/USB/HID/미디어**: Web API 및 Electron 권한 핸들링을 통한 장치 제어 서비스 구현 및 DI 등록 완료.

### 12. 애플리케이션 표시명 및 아이콘 설정
- **표시명(productName)**: 앱 설치 이름 및 창 제목 설정 완료.
